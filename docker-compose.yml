services:
  mam-audiofinder:
    # `up --build` uses the checked-out source; `pull` can use the published GHCR image.
    build: .
    image: ghcr.io/odrusso/mam-audiofinder-transmission:${IMAGE_TAG:-latest}
    container_name: mam-audiofinder
    ports:
      - "${APP_PORT}:8080"
    env_file:
      - .env
    user: "${PUID}:${PGID}"
    environment:
      UMASK: "${UMASK}"
    volumes:
      - ${DATA_DIR}:/data
      # Replace the host paths on the left before first run. Keep the in-container paths static.
      - /path/to/transmission-downloads:/downloads
      - /path/to/audiobook-library:/library
      - /path/to/ebook-library:/ebooks
    restart: unless-stopped
